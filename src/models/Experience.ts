export interface Experience {
    company: string
    duration: string
    position: string
    responsibilities: string[]
}

export interface Education {
    institution: string
    duration: string
    degree: string
    details: string[]
}